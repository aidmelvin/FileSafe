Pseudo code for everything:


Array all_competitors (dynamic)

Array aff_r1 (dynamic)
Array neg_r1 (dynamic)

Array aff_r2 (dynamic)
Array neg_r2 (dynamic)


Func master_function
{
	gather_competitors
	if there are only 2 competitors:
		#just write some code here to  automatically output them in each round, swapped
		#return
	set_aff_and_neg
	
	while  NOT check_equal_matchups
		aff_r2 = suffle_list(aff_r1)
		neg_r2 = shuffle_list(neg_r1)
}


Func gather_competitors
{
	#takes in list of competitors and fills in all_competitors
	#if total number is even, don't change anything
	#if total number is  odd, add "Bye" to list
}

Func set_aff_and_neg
{
	#while progress = half of all_competitors
	#randomly add people from all_competitors to off
	#add the rest of people to neg
}

Func shuffle_list ARGS: list
{
	#return shuffled list
	#its ok if the list is the same as last time
}

Func check_equal_matchups
{
	#return true if there is a repeating matchup between rounds
	#return false otherwise
}

Func are_lists_equal ARGS: list1, list2